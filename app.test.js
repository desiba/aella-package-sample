const chaiHttp = require('chai-http');
const chai, { expect } = require('chai');
const {fetch} = require('./index');
chai.use(chaiHttp);

describe('Test the index functions', () => {

    describe('fetch', () => {
        it('this test fetch() with error-code parameter', (done) => {
            
        });

        it('this test fetch() with error-code and error-message parameter', (done) => {
            
        });
    });

});
